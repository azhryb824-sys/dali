name: Build Dali Windows Desktop

on:
  workflow_dispatch:
    inputs:
      publish_update:
        description: Publish this version as a desktop update
        required: false
        default: false
        type: boolean
  push:
    branches: [godaddy-preview]
    tags: ["v*"]
    paths:
      - "desktop/**"
      - ".github/workflows/desktop-windows.yml"

permissions:
  contents: write

jobs:
  windows-installer:
    runs-on: windows-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: desktop/package-lock.json
      - name: Install desktop dependencies
        working-directory: desktop
        run: npm ci
      - name: Build single Windows installer
        working-directory: desktop
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if ("${{ startsWith(github.ref, 'refs/tags/v') || inputs.publish_update }}" -eq "true") {
            npx electron-builder --win nsis --x64 --publish always
          } else {
            npm run build:windows
          }
      - name: Verify installer
        shell: pwsh
        run: |
          $installer = Get-Item "desktop/dist/Dali-Desktop-Setup-0.2.0.exe"
          if ($installer.Length -lt 50000000) { throw "Installer is unexpectedly small" }
          Get-FileHash $installer.FullName -Algorithm SHA256
      - uses: actions/upload-artifact@v4
        with:
          name: Dali-Desktop-Setup-0.2.0
          path: |
            desktop/dist/Dali-Desktop-Setup-0.2.0.exe
            desktop/dist/latest.yml
            desktop/dist/*.blockmap
          if-no-files-found: error
          retention-days: 30
