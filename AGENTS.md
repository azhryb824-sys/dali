# قواعد مشروع دالي

## عقد الإشعارات الإلزامي

مركز الإشعارات جزء أساسي من معمارية النظام، وليس إضافة واجهة. عند تنفيذ أي ميزة أو تغيير برمجي مستقبلي في هذا المشروع، بما في ذلك المحادثات اللاحقة:

1. حلّل الأحداث الجديدة التي تتطلب معرفة المستخدم أو اتخاذ إجراء.
2. استدعِ `emitPortalNotification` من `lib/portal-notifications.ts` بعد نجاح كل حدث تشغيلي مهم، مع تحديد `module` و`severity` و`entityType` و`entityId` و`actionView` والجهة المستهدفة.
3. أضف التنبيهات الزمنية أو القابلة للحل إلى `refreshOperationalNotifications` باستخدام `dedupeKey` ثابت و`source: "system-check"` حتى تُحدّث تلقائياً ولا تتكرر.
4. لا تجعل الإشعار بديلاً عن التحقق من الصلاحية أو سجل النشاط؛ يجب أن تنجح العملية الأصلية أولاً، ثم يُسجَّل النشاط ويُنشأ الإشعار.
5. اجعل النقر على الإشعار يفتح القسم أو السجل المعني، وحدّث أنواع البحث الشامل إذا أُضيف كيان إداري جديد.
6. اختبر حالات غير المقروء، والتعليم كمقروء، والإخفاء، والتصفية، وصلاحيات الموظف والإدارة، وتزامن الإشعارات بعد كل تغيير.

الأحداث عالية الأولوية تشمل: طلبات الزوار، نجاح أو فشل الردود البريدية، نقص عمالة العقود، انتهاء الإقامات والمستندات، الاستحقاقات المالية، المواعيد القانونية، طلبات اعتماد المستخدمين، وأي فشل يمنع إكمال سير عمل أساسي.

## ثوابت المشروع

- الموقع العام منفصل عن `/portal` ولا يحتوي على رابط للبوابة.
- جميع صلاحيات البوابة تُتحقق من جهة الخادم.
- صور الموقع العام يجب ألا تحتوي على بشر أو حيوانات أو طيور أو نباتات أو أي كائن حي.
- واجهات الموقع والنظام بالعربية الفصحى وبخط Tajawal.
- البحث الشامل في البوابة والبحث في الموقع يجب أن يشملا أي قسم أو صفحة عامة تُضاف مستقبلاً.
- حقول مهنة العامل وجنسيته تبقى خيارات قابلة للبحث بالكتابة، وتُراجع قوائمها عند إضافة مهن أو جنسيات تشغيلية جديدة.

## تطابق نماذج الطلب والعرض والعقد

بتوجيه المستخدم: كل تعديل مستقبلي في نموذج العقد يستلزم تنفيذ التعديل الموافق له في نموذج عرض السعر ونموذج طلب عرض السعر، وفي التحقق والحفظ والتحويل بين المراحل وملفات PDF المرتبطة. لا يُحذف أي حقل من نموذج العقد لتحقيق التطابق. تحفظ الحقول غير المتاحة في مرحلة الطلب كاختيارية وتُستكمل قبل الإصدار أو الاعتماد.

المسار الإلزامي: طلب عرض سعر ← اعتماد المالك أو مشرف النظام ← إنشاء عرض سعر ← اعتماد المالك أو مشرف النظام ← المشاركة برقم العميل والتحويل إلى عقد. تُفرض هذه البوابات في الخادم وتُسجل قراراتها وإشعاراتها. رقم واتساب يُجلب من العميل أو المحادثة أو طلب عرض السعر المرتبط، ويُعرض للتعديل وتوحيد الصيغة قبل مشاركة العرض أو الفاتورة.
