# Release v0.2.1 - 2026-08-19

## إصلاحات تسجيل الدخول والتفويض

- إزالة رقم الهوية الثابت من الشفرة ومنع استخدام مدخل المستخدم لمنح دور إداري.
- توحيد `PORTAL_ADMIN_EMAIL` و`PORTAL_ADMIN_EMAILS` في مصدر إعداد واحد، مع اعتماد البريد الموثوق لمنح صلاحية المدير.
- إضافة `PORTAL_ADMIN_IDENTIFIER` صراحة إلى إعداد Render لإنشاء اعتماد المدير الأول بصورة آمنة.
- دعم إدخال رقم الهوية أو الإقامة بالأرقام العربية والهندية وتحويله على الخادم إلى أرقام إنجليزية قبل التحقق.
- إعادة أخطاء نموذج الدخول والاستعادة إلى صفحاتها برسائل عامة ورقم متابعة بدل عرض JSON أو تفاصيل داخلية.
- تثبيت أصل الروابط والكوكيز على نطاق HTTPS الخارجي خلف وكيل Render.

## إصلاحات قاعدة البيانات والتشغيل

- إزالة قاعدة SQLite الافتراضية الصامتة من تشغيل Render؛ يفشل التشغيل عند غياب `DATABASE_URL` أو عند استخدام مخطط غير مدعوم.
- اشتراط مسارات تخزين مطلقة لقاعدة الملف والمرفقات عند تشغيل Render.
- فصل `/api/health/live` عن `/api/health/ready`، مع إعادة `503` من فحص الجاهزية عند تعطل القاعدة أو نقص إعداد المصادقة.
- إبقاء `/api/health` فحص حياة متوافقًا مع إعدادات Render القديمة حتى لا تمنعها من تفعيل الإصدار قبل مزامنة مسار الفحص الجديد.
- إضافة فحص دخان خارجي يتحقق من مساري الحياة والجاهزية وصفحة الدخول على نطاق Render العام.

## التحقق قبل الدمج

- تشغيل TypeScript وESLint وبناء المشروع وجميع اختبارات الانحدار عبر GitHub Actions.
- التحقق من عدم وجود أرقام هوية ثابتة أو fallback صامت لقاعدة الإنتاج.
- التحقق من أن مساري الحياة يبقيان متاحين عند تعطل القاعدة بينما يفشل مسار الجاهزية بوضوح.

---

**الحالة**: مرشح للنشر بعد نجاح بوابة CI وفحص Render الخارجي.
