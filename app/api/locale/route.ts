import { isAppLocale, localeCookieName } from "@/lib/i18n";import{jsonNoStore,readLimitedJson,rejectCrossSiteRequest}from"@/lib/security";
export async function POST(request:Request){if(rejectCrossSiteRequest(request))return jsonNoStore({error:"مصدر الطلب غير مسموح"},{status:403});const parsed=await readLimitedJson(request,1000);if(!parsed.ok)return parsed.response;const locale=(parsed.value as{locale?:unknown}).locale;if(!isAppLocale(locale))return jsonNoStore({error:"اللغة غير مدعومة"},{status:400});const response=jsonNoStore({locale});response.headers.append("set-cookie",`${localeCookieName}=${locale}; Path=/; Max-Age=31536000; SameSite=Lax; Secure`);return response}
