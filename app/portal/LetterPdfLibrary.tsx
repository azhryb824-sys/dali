"use client";
import { useEffect, useState } from "react";
type Letter={id:number;referenceCode:string;subject:string;recipient:string;status:string};
export default function LetterPdfLibrary(){const[letters,setLetters]=useState<Letter[]>([]);useEffect(()=>{const controller=new AbortController();void fetch("/api/portal/letters",{cache:"no-store",signal:controller.signal}).then(async response=>{if(response.ok){const data=await response.json() as{letters?:Letter[]};setLetters(data.letters||[])}}).catch(()=>undefined);return()=>controller.abort()},[]);if(!letters.length)return null;return <section className="panel letter-pdf-library"><h2>نسخ الخطابات القابلة للتنزيل</h2><p>اختر العربية فقط أو النسخة العربية/الإنجليزية المقسومة طوليًا.</p><div className="feature-list compact">{letters.map(letter=><article key={letter.id}><div><strong>{letter.referenceCode} · {letter.subject}</strong><small>{letter.recipient} · {letter.status}</small></div><a href={`/api/portal/letters/${letter.id}/pdf?language=ar`} target="_blank">PDF عربي</a><a href={`/api/portal/letters/${letter.id}/pdf?language=bilingual`} target="_blank">PDF عربي/English</a></article>)}</div></section>}
