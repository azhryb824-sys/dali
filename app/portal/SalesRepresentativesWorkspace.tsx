"use client";import{FormEvent,useEffect,useMemo,useState}from"react";
type Representative={id:number;representativeCode:string;representativeType:"sales"|"purchasing";fullName:string;mobile:string;email:string|null;region:string;commissionBps:number;status:string;clientCount:number;contractCount:number;contractValueHalalas:number};type RequestRow={id:number;requestCode:string;representativeId:number;requestType:"sales"|"purchase";clientName:string|null;clientMobile:string|null;title:string;details:string;estimatedAmountHalalas:number;status:string;decisionReason:string|null;quoteVersionId:number|null};
export default function SalesRepresentativesWorkspace({canWrite}:{canWrite:boolean}){const[rows,setRows]=useState<Representative[]>([]),[requests,setRequests]=useState<RequestRow[]>([]),[isOwner,setIsOwner]=useState(false),[open,setOpen]=useState(false),[requestOpen,setRequestOpen]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState("");const load=()=>{void fetch("/api/portal/sales-representatives",{cache:"no-store"}).then(r=>r.json()as Promise<{representatives?:Representative[];requests?:RequestRow[];isOwner?:boolean;error?:string}>).then(data=>{if(!data.representatives)throw new Error(data.error);setRows(data.representatives);setRequests(data.requests||[]);setIsOwner(Boolean(data.isOwner))}).catch(e=>setError(e instanceof Error?e.message:"تعذّر تحميل المناديب"))};useEffect(load,[]);const sales=rows.filter(r=>r.representativeType==="sales"),purchasing=rows.filter(r=>r.representativeType==="purchasing");const pending=useMemo(()=>requests.filter(r=>["submitted","changes_requested"].includes(r.status)).length,[requests]);async function addRep(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);const response=await fetch("/api/portal/sales-representatives",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget).entries()))});const data=await response.json()as{error?:string};setBusy(false);if(!response.ok){setError(data.error||"تعذر الحفظ");return}setOpen(false);load()}async function submitRequest(event:FormEvent<HTMLFormElement>){event.preventDefault();setBusy(true);const form=event.currentTarget,fd=new FormData(form);const items=String(fd.get("itemLines")||"").split("\n").filter(Boolean).map(line=>{const[p,q,d,u,n]=line.split("|").map(v=>v.trim());return{profession:p,quantity:Number(q||1),durationMonths:Number(d||1),unitPrice:Number(u||0),notes:n||""}});const payload={...Object.fromEntries(fd.entries()),action:"submit",items};const response=await fetch("/api/portal/representative-requests",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(payload)});const data=await response.json()as{error?:string};setBusy(false);if(!response.ok){setError(data.error||"تعذر إرسال الطلب");return}setRequestOpen(false);load()}async function act(id:number,action:string,status?:string){let reason="";if(action==="decide"&&status!=="approved"){reason=window.prompt(status==="rejected"?"سبب الرفض النهائي":"التعديلات المطلوبة")||"";if(reason.length<10)return}setBusy(true);const response=await fetch("/api/portal/representative-requests",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action,id,status,reason})});const data=await response.json()as{error?:string};setBusy(false);if(!response.ok){setError(data.error||"تعذر تنفيذ القرار");return}load()}
const label:Record<string,string>={submitted:"بانتظار قرار المالك",changes_requested:"مطلوب تعديل",approved:"معتمد",rejected:"مرفوض نهائيًا",converted:"تم إنشاء عرض"};return <><div className="content-heading module-heading"><div><p className="admin-eyebrow">المبيعات والمشتريات الميدانية</p><h1>إدارة المناديب والطلبات</h1><span>مساران منفصلان للطلبات، مع اعتماد حصري للمالك أو مشرف النظام وربط بالمبيعات والمالية.</span></div><div className="heading-actions">{canWrite&&<button className="admin-secondary" onClick={()=>setRequestOpen(true)}>+ إرسال طلب مندوب</button>}{canWrite&&isOwner&&<button className="admin-primary" onClick={()=>setOpen(true)}>+ إضافة مندوب</button>}</div></div>{error&&<p className="operations-notice">{error}</p>}<section className="representative-kpis"><article><span>مناديب المبيعات</span><strong>{sales.length}</strong><small>طلبات عملاء وعروض</small></article><article><span>مناديب المشتريات</span><strong>{purchasing.length}</strong><small>طلبات موردين وشراء</small></article><article><span>تنتظر قرارًا</span><strong>{pending}</strong><small>للمالك/المشرف فقط</small></article><article><span>طلبات محولة</span><strong>{requests.filter(r=>r.status==="converted").length}</strong><small>إلى عروض سعر</small></article></section><section className="panel representative-request-board"><header><div><h2>صندوق طلبات المناديب</h2><p>اعتماد، رفض نهائي، إعادة للتعديل، ثم إنشاء عرض السعر أو متابعة الشراء.</p></div></header><div className="representative-request-list">{requests.map(item=>{const rep=rows.find(r=>r.id===item.representativeId);return <article key={item.id}><span className={`request-kind ${item.requestType}`}>{item.requestType==="sales"?"مبيعات":"مشتريات"}</span><div><strong>{item.requestCode} — {item.title}</strong><p>{item.clientName||"طلب شراء"} · {rep?.fullName}</p><small>{item.details}</small>{item.decisionReason&&<em>{item.decisionReason}</em>}</div><b>{new Intl.NumberFormat("ar-SA",{style:"currency",currency:"SAR"}).format(item.estimatedAmountHalalas/100)}</b><span className={`workflow-status ${item.status}`}>{label[item.status]||item.status}</span><div className="representative-request-actions">{isOwner&&["submitted","changes_requested"].includes(item.status)&&<><button onClick={()=>void act(item.id,"decide","approved")}>اعتماد</button><button onClick={()=>void act(item.id,"decide","changes_requested")}>طلب تعديل</button><button className="danger-action" onClick={()=>void act(item.id,"decide","rejected")}>رفض نهائي</button></>}{isOwner&&item.requestType==="sales"&&item.status==="approved"&&<button className="admin-primary" onClick={()=>void act(item.id,"create-quote")}>إنشاء عرض سعر</button>}{item.quoteVersionId&&<span>عرض مرتبط #{item.quoteVersionId}</span>}</div></article>})}</div></section><section className="panel"><div className="panel-head"><div><h2>سجل المناديب</h2><p>{rows.length} مندوب</p></div></div><div className="management-table-wrap"><table className="management-table representatives-table"><thead><tr><th>المندوب</th><th>النوع</th><th>التواصل</th><th>المنطقة</th><th>العملاء</th><th>العقود</th><th>قيمة العقود</th><th>الحالة</th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td><strong>{row.fullName}</strong><small dir="ltr">{row.representativeCode}</small></td><td>{row.representativeType==="sales"?"مندوب مبيعات":"مندوب مشتريات"}</td><td><strong dir="ltr">{row.mobile}</strong><small dir="ltr">{row.email||"—"}</small></td><td>{row.region}</td><td>{row.clientCount}</td><td>{row.contractCount}</td><td>{new Intl.NumberFormat("ar-SA",{style:"currency",currency:"SAR"}).format(row.contractValueHalalas/100)}</td><td>{row.status==="active"?"نشط":"غير نشط"}</td></tr>)}</tbody></table></div></section>{open&&<div className="modal-layer"><button className="drawer-backdrop" onClick={()=>setOpen(false)}/><section className="record-modal"><div className="drawer-head"><div><span>تعريف الصلاحية الميدانية</span><h2>إضافة مندوب</h2></div><button onClick={()=>setOpen(false)}>×</button></div><form onSubmit={addRep}><label>نوع المندوب<select name="representativeType"><option value="sales">مندوب مبيعات</option><option value="purchasing">مندوب مشتريات</option></select></label><label>الاسم<input name="fullName" required/></label><label>الجوال<input name="mobile" required/></label><label>البريد المرتبط بحسابه<input name="email" type="email" required/></label><label>الهوية<input name="nationalId" pattern="[0-9]{10}"/></label><label>المنطقة<input name="region" defaultValue="مكة المكرمة"/></label><label>العمولة %<input name="commissionPercent" type="number" min="0" max="100" step=".01"/></label><button className="admin-primary" disabled={busy}>حفظ</button></form></section></div>}{requestOpen&&<div className="modal-layer"><button className="drawer-backdrop" onClick={()=>setRequestOpen(false)}/><section className="record-modal representative-request-modal"><div className="drawer-head"><div><span>طلب ميداني جديد</span><h2>إرسال طلب مندوب</h2></div><button onClick={()=>setRequestOpen(false)}>×</button></div><form onSubmit={submitRequest}><label>المندوب<select name="representativeId" required>{rows.filter(r=>r.status==="active").map(r=><option key={r.id} value={r.id}>{r.fullName} — {r.representativeType==="sales"?"مبيعات":"مشتريات"}</option>)}</select></label><label>عنوان الطلب<input name="title" required/></label><label>اسم العميل (للمبيعات)<input name="clientName"/></label><label>جوال العميل<input name="clientMobile"/></label><label>الموقع<input name="workSite"/></label><label>القيمة التقديرية<input name="estimatedAmount" type="number" min="0" step=".01"/></label><label className="span-two">التفاصيل<textarea name="details" required minLength={10}/></label><label className="span-two">البنود: الاسم | الكمية | المدة | السعر | الملاحظات<textarea name="itemLines" rows={5} placeholder="عامل نظافة | 10 | 12 | 2500 | وردية صباحية"/></label><button className="admin-primary span-two" disabled={busy}>إرسال للمالك/مشرف النظام</button></form></section></div>}</>}
