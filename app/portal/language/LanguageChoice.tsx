"use client";import{useState}from"react";import{useRouter}from"next/navigation";import{AppLocale,localeNames}from"@/lib/i18n";
export default function LanguageChoice(){const[locale,setLocale]=useState<AppLocale>("ar"),[busy,setBusy]=useState(false),router=useRouter();async function save(){setBusy(true);const response=await fetch("/api/portal/language",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({locale})});setBusy(false);if(response.ok)router.replace("/portal")}return <main className="language-onboarding"><section><p>WELCOME · خوش آمدید</p><h1>اختر لغة النظام</h1><span>Choose your system language · نظام کی زبان منتخب کریں</span><div>{(["ar","en","ur"]as AppLocale[]).map(item=><button key={item} className={locale===item?"active":""} onClick={()=>setLocale(item)}><b>{item==="ar"?"ع":item==="en"?"EN":"اُ"}</b><strong>{localeNames[item]}</strong><small>{item==="ar"?"واجهة عربية":item==="en"?"English interface":"اردو انٹرفیس"}</small></button>)}</div><button className="language-save" disabled={busy} onClick={()=>void save()}>{busy?"جارٍ الحفظ...":"متابعة وحفظ اللغة"}</button></section></main>}
