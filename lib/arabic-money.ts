const ones=["","واحد","اثنان","ثلاثة","أربعة","خمسة","ستة","سبعة","ثمانية","تسعة","عشرة","أحد عشر","اثنا عشر","ثلاثة عشر","أربعة عشر","خمسة عشر","ستة عشر","سبعة عشر","ثمانية عشر","تسعة عشر"];
const tens=["","","عشرون","ثلاثون","أربعون","خمسون","ستون","سبعون","ثمانون","تسعون"];
const hundreds=["","مائة","مائتان","ثلاثمائة","أربعمائة","خمسمائة","ستمائة","سبعمائة","ثمانمائة","تسعمائة"];
function underThousand(value:number){const parts:string[]=[];const h=Math.floor(value/100);const rest=value%100;if(h)parts.push(hundreds[h]);if(rest){if(rest<20)parts.push(ones[rest]);else{const unit=rest%10;if(unit)parts.push(ones[unit]);parts.push(tens[Math.floor(rest/10)]);}}return parts.join(" و");}
function groupLabel(value:number,singular:string,dual:string,plural:string){if(value===1)return singular;if(value===2)return dual;if(value>=3&&value<=10)return `${underThousand(value)} ${plural}`;return `${underThousand(value)} ${singular}`;}
export function integerToArabicWords(value:number){const integer=Math.max(0,Math.floor(value));if(integer===0)return "صفر";const parts:string[]=[];const billions=Math.floor(integer/1_000_000_000);const millions=Math.floor((integer%1_000_000_000)/1_000_000);const thousands=Math.floor((integer%1_000_000)/1000);const remainder=integer%1000;if(billions)parts.push(groupLabel(billions,"مليار","ملياران","مليارات"));if(millions)parts.push(groupLabel(millions,"مليون","مليونان","ملايين"));if(thousands)parts.push(groupLabel(thousands,"ألف","ألفان","آلاف"));if(remainder)parts.push(underThousand(remainder));return parts.join(" و");}
export function halalasToArabicWords(halalas:number){const safe=Math.max(0,Math.round(halalas));const riyals=Math.floor(safe/100);const fraction=safe%100;return `${integerToArabicWords(riyals)} ريال سعودي${fraction?` و${integerToArabicWords(fraction)} هللة`:""} فقط لا غير`;}
